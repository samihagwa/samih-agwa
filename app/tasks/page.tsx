import { SectionOverview } from "../../components/sections/SectionOverview";
import { sections } from "../../lib/sections";
export default function TasksPage() { return <SectionOverview config={sections.tasks} />; }
